package JogoCartasRPG.Service;

import JogoCartasRPG.Elementos;
import JogoCartasRPG.Util.Gerador.GeradorCpu;

public class Niveis {
    Elementos elementos = new Elementos();
    GeradorCpu geradorCpu = new GeradorCpu();

    public void combinacoesDecartas() {
//
//        if (player1.equals(Elementos.getGelo()) && player1.equals(Elementos.getFogo())) {
//            System.out.println(" Voce combinou dois elementos total de forca" + 7);
//        }
//        if (player1.equals(Elementos.getFogo()) && geradorCpu.equals(Elementos.getTerra())) {
//            System.out.println(" VOCE GANHOU ");
//        }
//        if (player1.equals(Elementos.getGelo()) && geradorCpu.equals(Elementos.getGelo())) {
//            System.out.println(" VOCE PERDEU ");
        }

    }

